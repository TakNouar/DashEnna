# backend/ (not started)

This is a placeholder. There is no backend code yet — see
`../docs/ROADMAP.md` (Phase 2) for the plan: Node/Express + SQLite, real
password hashing, server-side permission enforcement, and an API for the
frontend to replace its current `localStorage`-only data layer.
